@model Hamburguesas.Areas.Admin.Models.ViewModels.PromocionViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "QuitarPromo";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<form method="post" action="~/admin/quitarpromocion">
    <h1>Eliminar promoción</h1>

    <span class="pregunta"><b>¿Desea eliminar la promoción de la hamburguesa?</b></span>
    <span>
        <label for="nombre">Nombre:</label>
        <input type="text" id="nombre" required readonly asp-for="Nombre" >
    </span>
    <span>
        <label for="precio">Precio real:</label>
        <input type="text" id="precio" required readonly asp-for="Precio">
    </span><span>
        <label for="promocion">Precio promocion:</label>
        <input type="text" id="promocion" required readonly asp-for="PrecioPromo">
    </span>
    <span class="error">@Html.ValidationSummary()</span>

    <div class="botones">
        <input type="submit" value="Si" />
        <a href="~/admin/menu/menu">
            <input type="button" value="No" />
        </a>
    </div>
</form>

<script>

    function previewImagen() {
        const inputImagen = document.getElementById('imagen');
        const imagenPreview = document.getElementById('imagen-preview');

        const archivoImagen = inputImagen.files[0];

        if (archivoImagen) {
            const lector = new FileReader();

            lector.onload = function (e) {
                imagenPreview.src = e.target.result;
                imagenPreview.style.display = 'block';
            };

            lector.readAsDataURL(archivoImagen);
        } else {
            imagenPreview.src = '#';
            imagenPreview.style.display = 'none';
        }
    }
</script>

