@model Hamburguesas.Areas.Admin.Models.ViewModels.EliminarMenuViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Eliminar";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<form method="post" action="~/admin/eliminar">
    <h1>Eliminar hamburguesa del menú</h1>

    <span class="pregunta"><b>¿Desea eliminar la hamburguesa?</b></span>
    <span>
        <label for="nombre" >Nombre:</label>
        <input type="text" id="nombre" required asp-for="Nombre" readonly>
    </span>
    <span class="error">@Html.ValidationSummary()</span>

    <div class="botones">
        <input type="submit" value="Si" />
        <a href="~/admin/menu/menu">
            <input type="button" value="No" />
        </a>
    </div>
</form>

<script>

    function previewImagen() {
        const inputImagen = document.getElementById('imagen');
        const imagenPreview = document.getElementById('imagen-preview');

        const archivoImagen = inputImagen.files[0];

        if (archivoImagen) {
            const lector = new FileReader();

            lector.onload = function (e) {
                imagenPreview.src = e.target.result;
                imagenPreview.style.display = 'block';
            };

            lector.readAsDataURL(archivoImagen);
        } else {
            imagenPreview.src = '#';
            imagenPreview.style.display = 'none';
        }
    }
</script>

